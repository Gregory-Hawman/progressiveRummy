export default function Draw(drawnCard, round, playerCount) {
    // current player draws a card
    let i = 0;
    // for (i; i < playerCount; i++){
    //   if(playersList[i].currentPlayer){
    //     console.log(drawnCard)
    //     let takenCard = [drawnCard]
    //     console.log(takenCard)
    //     playersList[i].hand.splice(0, 0, takenCard)
    //   }
    // }
  
    
  };
function EndTurn() {
    // end current players turn and move to the next player
    // if current player can go out, the run last card phase, else move to next player.
    if (playersList.currentPlayer){
  
    }
  }